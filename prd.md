# 밤 판매 사이트 — 재사용 제작 프롬프트

아래 사양대로 **무통장 입금 전용 밤 판매 사이트**를 만들어라. 운영자는 기술에 서툰 고령의 아버지이며,
최소 조작으로 주문·발송을 처리하는 것이 목표다. (아래는 실제 구현 기준 요약)

## 디자인
심플 하면서 과하지 않은 파스텔풍의 귀여운 스타일

## 기술 스택
- 프론트엔드: 모바일 웹에 맞는 화면 구성
- 백엔드: **Firebase** — Firestore(DB), App hosting, **Blaze** 요금제. 프로덕션 모드에서 시작.
- 도메인 없이 `*.web.app` 무료 주소 사용.

## 페이지
- `/` 상품 목록(담기), `/order` 주문 결제, `/track` 배송조회, `/admin` 관리자(전용).
- 상품 목록 : 임시로 사진 이미지와 가격(추후 업데이트 예정) 보여주고 수량 선택 및 담기 기능. 목록은 '대보 중', '대보 대', '대보 특대', '포르단 중', '포르단 대', '포르단 특대', '옥광 중', '옥광 대', '옥광 특대'. 재고 소진시 매진 표시


## 주문/결제 (`/order`)
- **받는 사람(수령인) 이름·전화·주소** + **입금자명** 입력.
- **"입금자와 수령인 동일" 체크 버튼** → 체크 시 수령인 자동 입력.
- 주문 생성은 `createOrder` 함수가 처리(상품 가격을 **서버에서 재계산**해 조작 방지), 상태 `입금대기`.
- 완료 화면에 입금 계좌 안내(관리자 설정값) + 금액 표시.

## 입금 자동 매칭 (핵심)
- 아버지 폰의 **MacroDroid**가 입금 문자에서 **입금액·입금자명·은행명**만 추출(서버는 원문 파싱 안 함, 문자 샘플 불필요).
- 서버 매칭: **1건**=`발송대기` 확정 / **다수(동명이인)**=`확인필요` / **0건**=`미매칭`.
- 응답 문구를 MacroDroid가 알림으로 표시.

## 배송조회 (`/track`)
- 로그인 없이 **입금자+전화**로 조회. 서버 `lookupOrders` 함수가 정확 일치 건만 반환(직접 쿼리 금지).
- **취소·삭제 주문은 숨김**. 교환/환불 상태는 진행 상태로 노출.
- 우체국 택배 연동하여 송장번호 클릭시 배송상태 표시

## 관리자 (`/admin`) — 아버지용, 극단적 단순
- **접근 = 공유 비밀번호 하나**(계정 없음). 폰은 1회 로그인 후 세션 유지 + PWA 홈아이콘.
- **매칭 예외 강조**: `확인필요`/`미매칭` 미해결 입금을 화면 최상단 빨간 영역으로 강조(확인필요는 후보 중 선택).
- **발송대기 목록**: 각 **[발송완료]** 버튼(+송장번호 선택).
- **판매 현황 차트(간단)**: 최근 7일/전체 매출, 상품별 판매량, 상태별 건수 타일(클라이언트 집계).
- **전체 수정**: 이름·전화·주소·상품/수량/가격·입금자명·상태등 직접 수정(합계 자동 재계산), **취소/삭제**.
- **교환·환불**: 상태 `환불요청/환불완료`·`교환요청/교환완료` + 메모·환불액(무통장이라 수동 송금 후 상태 변경).
- **재고 관리**: 상품 목록의 재고 수량을 미리 설정하고 재고의 20%만 남은 경우와 모두 소진된 경우 관리자 페이지에서 강조하여 보여줘야 함.
- 상품 관리(추가/수정/숨김/삭제), 설정(입금 계좌,전화번호).

## .env.local을 위한 App Hosting의 key값
apiKey: "AIzaSyBSqyVR6Jz_xJVLHTBCDu_gYCb8-nYTGRU",
authDomain: "albam-416fd.firebaseapp.com",
projectId: "albam-416fd",
storageBucket: "albam-416fd.firebasestorage.app",
messagingSenderId: "653376312477",
appId: "1:653376312477:web:61626c04a2385bd85bb074"

## 배포
https://github.com/skellee80/albam 감지하여 변경시 자동 빌드
