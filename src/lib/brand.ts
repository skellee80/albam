/**
 * 가게 이름.
 *
 * 이름이 헤더·브라우저 탭·홈화면 아이콘·관리자 화면에 흩어져 있어서 한 곳에 모은다.
 * 나중에 상호가 바뀌면 이 파일만 고치면 된다.
 */
export const BRAND = {
  /** 정식 이름. 헤더·브라우저 탭에 그대로 쓴다. */
  full: '칠갑산 석촌 햇 밤',

  /**
   * 자리가 좁은 곳에 쓰는 짧은 이름.
   * 홈화면 아이콘 밑에 붙는 글자는 길면 잘려서 알아볼 수 없다.
   */
  short: '칠갑산 햇밤',
} as const;
